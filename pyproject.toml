[project]
name = "fairsharer"
version = "0.1.0"
description = "Fair sharing algorithm example"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 88

[dependency-groups]
dev = [
    "pytest",
    "ruff"
]